# Default clone source for remote install.sh (override with env or --repo).
# Replace YOUR_ORG when publishing the standalone template repository.
AGENT_MEMORY_INSTALL_REPO=https://github.com/v3gaS/agent-memory.git
AGENT_MEMORY_INSTALL_REF=main

# Example stacks (used by install.sh --preset)
# PRESET_PYTHON_TEST=pytest -q
# PRESET_NODE_TEST=npm test
# PRESET_GO_TEST=go test ./...
