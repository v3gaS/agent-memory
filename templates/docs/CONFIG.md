---
Last reconciled: {{DATE}}
Scope: Configuration keys, env vars, resolution order.
---

# CONFIG — {{PROJECT_NAME}}

**Primary config file:** `{{PRIMARY_CONFIG}}`

**Conflict order:** code defaults → YAML/config file → environment → runtime overrides (document your project's order below).

---

## 1. Resolution order

TODO: e.g. model defaults → YAML → `.env` → CLI flags.

---

## 2. Key reference

| Key / env | Type | Default | Purpose |
| --- | --- | --- | --- |
| TODO | TODO | TODO | TODO |

---

## 3. Modes / profiles

TODO: dev, staging, production — or N/A.

---

## 4. Changelog

- **{{DATE}}** — Initial config stub.
